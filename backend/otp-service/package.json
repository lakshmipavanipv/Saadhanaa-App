{
  "name": "soulsync-otp-service",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker that sends + verifies OTPs for Soulsync (email via Resend, SMS via Twilio).",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240620.0",
    "typescript": "^5.5.0",
    "wrangler": "^3.60.0"
  }
}
